import { css } from 'lit';

const baseStyle = css`
   :host {
        display: block;
        border: solid 1px gray;
        padding: 16px;
        max-width: 800px;
      }
`;

export const styles = [baseStyle];